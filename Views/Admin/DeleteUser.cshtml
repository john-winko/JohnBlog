@model BlogUser

@{
    ViewBag.Title = "title";
    Layout = "_Layout";

    System.Diagnostics.Debug.Assert(Model != null, nameof(Model) + " != null");
}
<h1>Delete</h1>

<h3>
    Are you sure you want to delete @Model.FirstName @Model.LastName (@Model.Email)?
</h3>
<div>
 <form asp-action="DeleteUser">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="GetRoles">Back to List</a>
    </form>
</div>